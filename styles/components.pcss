@layer components {
  .primary-button {
    @apply w-max transform rounded-full bg-black p-4 text-xl font-extrabold text-light transition-all duration-200 ease-linear hover:scale-110 dark:bg-light dark:text-primary-blue;
  }
}
